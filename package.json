{
  "name": "small-page",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "install-all": "npm run install-frontend && npm run install-backend",
    "install-frontend": "cd frontend && npm install && npm run build",
    "install-backend": "cd backend && npm run clean && npm install && npm run build",
    "start-firstime": "npm run install-all && npm run start",
    "start": "npm run start-backend && npm run start-frontend",
    "start-frontend": "cd frontend && npx serve -s build",
    "start-backend": "cd backend && npm run start"
  },
  "keywords": [],
  "author": "",
  "license": "ISC"
}
